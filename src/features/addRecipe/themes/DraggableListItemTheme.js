import { createTheme } from "@mui/material/styles";

/**
 * The common theme to apply to all components in the DraggableListItem
 */
export const DraggableListItemTheme = createTheme({
  palette: {
    camouflage: {
      main: "#ffffff",
    },
  },
});
